# Included inside the default EB server block (server { ... }). The default
# config sets `root /var/app/current/public;` and serves $uri directly; we add
# a fallback so non-file paths route to index.php for the PHP router to handle.
location / {
    try_files $uri $uri/ /index.php?$query_string;
}
